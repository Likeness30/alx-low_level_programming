#include "main.h"

/**
 * print_array - Function name
 * @a: 1st parameter
 * @n: 2nd parameter
 */

void print_array(int *a, int n)
{
int b;

for (b = 0; b < n; b++)
{
printf("%d", b[a]);
{
	if (b < n - 1)
		printf(", ");
}
	printf("\n");
}
}
